// Cloud accounts (Supabase). Leave both empty to run the app in on-device
// mode with no login, exactly as before. See docs/SETUP-ACCOUNTS.md.
//
// The anon key is meant to be public: every table is protected by row-level
// security, and all personal data is encrypted on the device before upload.
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
